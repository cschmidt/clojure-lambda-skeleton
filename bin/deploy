#!/bin/bash
# SCRIPT_PATH=$( cd "$(dirname "${BASH_SOURCE}")" ; pwd -P )
# $SCRIPT_PATH/package
aws --profile personal \
  lambda update-function-code \
  --function-name api-test \
  --zip-file fileb://target/lambda-webthumbs.zip
